var number=32;
console.log(number);
//here we see that by default javascript can print/display the output in form of decimal
//but we can chage the output in different base using toString("base") method
//e.g. 
console.log(number.toString("2")); //base 2 representation
console.log(number.toString("10")); //base 10 representation
console.log(number.toString("8"));
console.log(number.toString("16"));